# Chat Bridge 验证记录与发布验收清单

日期：2026-09-18。对应 [技术方案 1.4](technical-plan.md) 与 [菜单栏交互规格](menubar-interaction.md)。

2026-09-19 消息卡更新：[Markdown 与左右布局验证](verification/message-markdown-2026-09-19.md)。浮窗和看板均支持 Markdown，用户灰底靠右、AI 靠左；48 项原生测试、10 项应用包检查通过，深浅色合成内容已渲染检查。更新后原会话表格可结构化显示，两条消息通道仍连接。本轮不重新证明手机投递或 Agent 任务执行。

2026-09-19 新增：[原应用项目与会话接入验证](verification/native-catalog-2026-09-19.md)。Codex／Claude Code 本机目录、同 ID 续聊、命令指南、115 项服务测试、39 项原生测试和 8 项包内检查已记录；后续历史段落保留原执行时点的范围。

2026-09-19 编号交互更新：[项目与会话编号选择验证](verification/numbered-menus-2026-09-19.md)。项目／会话列表只显示编号与名称，回复编号选择；选择项目后自动列出会话。146 项服务测试通过，覆盖分页、来源隔离、过期、重启恢复和数字不误发；手机上的新版完整交互另行验收。

2026-09-19 看板入口更新：[浮窗入口与 Dock 验证](verification/dashboard-dock-2026-09-19.md)。浮窗右上角 ↗ 打开看板，看板可见时显示 Dock 图标；42 项原生测试通过，包含窗口复用、关闭、最小化恢复和草稿保留。

2026-09-19 iMessage 无回复排查：[恢复与签名验证](verification/imessage-recovery-2026-09-19.md)。本机读取返回 EPERM，系统设置中当前权限关闭。重连诊断与入口、绑定／游标保留已实现；148 项服务测试、42 项原生测试、10 项包检查及稳定开发签名检查通过。系统授权与手机编号交互尚未完成。

2026-09-19 权限消息提醒：[本轮验证](verification/imessage-notice-2026-09-19.md)。读取失权时通过独立发送端向已绑定手机号提醒一次；158 项服务测试和 10 项包检查通过。本机实际 EPERM 下提交成功，手动重连未重复；手机送达仍待用户确认，不代表收件权限已恢复。

2026-09-19 自动重连修复：[本轮记录](verification/imessage-auto-reconnect-2026-09-19.md)。权限已开启但原进程未重新打开数据库；手动恢复后 5 个不同来信各回复一次，用户确认编号项目列表已送达。现已增加每 5 秒恢复和解绑取消保护，162 项服务测试通过；同一签名更新后 iMessage 自动连接。此确认只覆盖项目列表送达，不能替代两级选择及完整任务验收。

**研究核验和本机只读检查的既有结果为 23 项通过、1 项环境入口不可用。现已有原生开发预览和核心测试，见下方实施验证；下面的 97 项完整产品验收仍未完整执行，不能被理解为真实通道／桌面集成已通过。原研究结果保留其执行时点含义。**

## 0.3.0 Codex 运行时范围调整

2026-09-19 iMessage 设置补充：本机填写值记忆、重启恢复、消息应用打开和未授权提示已检查；辅助功能授权后的设置直达尚待检查。结果和系统接口限制见 [配置辅助验证](verification/imessage-settings-2026-09-19.md)，不计作 iMessage 手机收发验收。

2026-09-19 配对诊断补充：已复现当前应用读取 Messages 被 `EPERM` 拒绝，TCC 日志显示旧签名要求不匹配；系统开关开启不等于当前版本已获权限。错误分类与四步配对引导已更新，13 项 iMessage 测试通过；完整回归及一个窗口命中测试失败的边界见 [配对诊断记录](verification/imessage-pairing-2026-09-19.md)。真实配对仍待当前版本授权和手机操作。

随后按用户要求改为「开始配对直接发请求 → 手机回复 OK → 自动启用」，取代上述四步流程。新回归结果和剩余真机检查见 [OK 配对验证](verification/imessage-ok-pairing-2026-09-19.md)，历史记录保持原执行时点。

用户已明确批准使用本机官方 App Server、由 Chat Bridge 管理会话，见 [ADR 003](decisions/003-managed-codex-runtime.md)。旧表 D01／D02 中“必须在既有桌面应用中双向可见”的要求保留为后续桌面接管验收，不再阻断当前独立运行时阶段。

新增实际检查：运行时登录握手、无项目会话创建和模型回复、同 ID 连续对话、跨进程恢复原上下文、等待原生开始事件后停止。五项真机检查通过，见 [证据 JSON](verification/codex-runtime-2026-09-18.json)。队列／审批自动化与包内验证见 [第三阶段记录](verification/phase3-2026-09-18.md)。

微信 `/status` 的成功由用户报告。修复 uint64 消息 ID 后已记录真实微信任务完成及回传提交；指定测试口令的手机送达仍待确认，见第三阶段记录。真实 iMessage、手机审批、锁屏、Claude 和原桌面接管仍分别待验收。以下历史数量和结果保留原执行时点。

## 0.1.0 第一阶段实施验证

第一阶段使用真实 Swift 原生包、随包 Node 和 SQLite；Agent 执行边界使用测试夹具，与实际 Codex／Claude 控制严格区分。历史结果与范围见 [实施验证记录](verification/implementation-2026-09-18.json)、[构建日志](verification/build-2026-09-18.log)、[应用包检查](verification/bundle-smoke-2026-09-18.json)。

检查分为 TypeScript 行为／IPC 测试、Swift 帧解码／菜单几何测试、应用包独立进程检查、原生配置界面操作。单元测试覆盖某条产品要求，不等于该条真实设备验收已经完成；因此下方 97 项保留待执行。

已经实际打开 Chat Bridge 的配置和聊天面板，验证安装版本、默认选项、Coming soon、固定上限、排序、名称、保活偏好重启恢复及未验证时禁用发送。电脑控制工具没有提供本应用菜单栏组的可操作快照，窗口截图也不足以证明跨窗口磨砂；这两类目视检查不计为通过。

G0 的额外阻断是电脑控制工具禁止操作 Codex 应用。没有通过其他 UI 自动化方法绕过该限制，也没有另起 Agent 引擎代替同一桌面 Session 验证。

## 0.2.0 第二阶段补充

已增加通道协议、持久化入站／发件箱、配对、凭据保护、继续／取消和原生配置界面。结果与边界以 [第二阶段验证](verification/phase2-2026-09-18.md)、[构建日志](verification/build-phase2-2026-09-18.log)、[包内进程检查](verification/bundle-smoke-phase2-2026-09-18.json) 为准。

微信测试使用真实社区协议客户端与模拟 HTTP；iMessage 测试使用真实 SDK 和隔离数据库，配对状态使用模拟消息。Swift 凭据测试验证输入与状态，不等于真实 Keychain 授权验收。以下完整产品的“阻断”检查不会因为这些自动化测试通过而自动变为通过。

## 授权后的追加验证

用户完成 macOS 授权后，微信重连、同版本重启恢复及设置面板退出快捷键已通过。真实手机消息往返与桌面 Agent 执行仍单独验收，详见 [追加记录](verification/authorization-2026-09-18.md)。

## 1. 状态口径和证据

| 状态 | 含义 |
| --- | --- |
| 已通过：源码／元数据 | 固定版本中存在相应字段、行为或发布记录；不证明完整运行效果。 |
| 已通过：本机只读 | 命令确实执行成功或应用确实存在；不推导网络、权限或桌面自动化成功。 |
| 已通过：交互示意 | 浏览器中的本地模拟交互通过；不代表原生窗口、真实 Agent 或通道集成通过。 |
| 入口不可用 | 实际探测失败；本轮没有修改环境来绕过。 |
| 待执行 | 用例和通过标准已定义，应用实现后执行。 |
| 不适用 | 必须记录原因，并且产品没有宣称该项能力；不能用来跳过核心要求。 |

实际证据文件为 [evidence-2026-09-18.json](research/evidence-2026-09-18.json)，包含 UTC 时间、系统／架构、实际应用版本、源码 commit、下载内容 SHA-256、npm 元数据和逐项结果。源码断言是静态检查，未运行第三方库测试，也未安装依赖。

复核命令，在仓库根目录运行：

```sh
python3 docs/research/verify_research.py --output /tmp/chat-bridge-research-latest.json
```

脚本访问公开源码／包元数据，读取应用版本，运行 CLI 帮助、版本、协议生成和既有服务版本探测。不会登录微信、读取聊天正文／凭据、发送消息、创建 Agent Session、启动 daemon、锁屏或修改系统设置。它会在临时目录生成并清理协议 schema。

脚本退出码 0 表示研究断言没有失败；仍需查看 `unavailable`，它不代表应用或发布验收通过。Node 24 发布文件存在也不表示本机已安装该版本；本机 Node 实际为 22.22.3。

## 2. 本轮已经验证通过的检查

| 证据 ID | 检查项 | 实际结果与边界 |
| --- | --- | --- |
| `DEP:chat` | chat 4.40.0 的发布元数据 | 已通过：包版本存在，声明 MIT、Node ≥20；未验证整包运行。 |
| `DEP:@photon-ai/imessage-kit` | imessage-kit 3.0.0 的发布元数据 | 已通过：版本、MIT、Node 要求存在；Node 下 SQLite 可选依赖已记录。 |
| `DEP:chat-adapter-weixin` | 微信适配器 0.1.4 的发布元数据 | 已通过：MIT、Node ≥22、chat peer 版本记录完整；不是腾讯官方适配器认证。 |
| `DEP:better-sqlite3` | better-sqlite3 12.11.1 的发布元数据 | 已通过：MIT、声明支持 Node 24；未验证签名后的原生模块加载。 |
| `SRC-01` | Chat SDK 的文本降级处理 | 已通过：源码省略 Actions，不能自动保留按钮操作。 |
| `SRC-02` | 微信卡片能力声明 | 已通过：能力表声明不支持 cards／buttons，支持私聊。 |
| `SRC-03` | Kit 的发送接口 | 已通过：收件人、文本、附件字段存在；不证明任意格式附件均可送达。 |
| `SRC-04` | Kit 的 Node SQLite 路径 | 已通过：源码引用 better-sqlite3；需在产品中显式打包。 |
| `SRC-05` | Photon 适配器本机模式 | 已通过：当前固定源码拒绝已移除的 local 模式。 |
| `SRC-06` | 社区微信适配器接入方式 | 已通过：README 存在 iLink 与 QR 登录说明。 |
| `SRC-07` | 腾讯协议的关键字段 | 已通过：cursor、context_token、client_id 均有协议记录；不证明服务端严格幂等。 |
| `SRC-08` | 腾讯 LICENSE 原文 | 已通过：原文声明 MIT 及保留声明要求；没有以 GitHub 自动识别结果代替原文。 |
| `SRC-09` | Node 24.21.0 macOS 发布物 | 已通过：官方校验清单包含 arm64 和 x64 文件；没有下载／执行运行时。 |
| `SRC-10` | 微信轮询源码的处理顺序 | 已通过：processInbound 位于保存 cursor 之前；不证明整个回调链持久化。 |
| `SRC-11` | Kit watcher 的重启起点 | 已通过：启动时取当前最大 ROWID；产品需要额外恢复补扫。 |
| `SRC-12` | Kit 发送返回及默认重试 | 已通过：Promise<void> 与三次默认尝试存在；不能作为已送达证明。 |
| `LOCAL-01` | 已安装桌面应用发现 | 已通过：Codex bundle `com.openai.codex`、Claude bundle `com.anthropic.claudefordesktop` 存在。 |
| `LOCAL-02` | Codex CLI 版本 | 已通过：`0.155.0-alpha.9`。 |
| `LOCAL-03` | Codex proxy 命令 | 已通过：帮助中提供 `--sock`；不代表有可连接服务。 |
| `LOCAL-04` | Codex 协议导出 | 已通过：生成 310 个 JSON schema；所需 thread／turn 参数字段存在。 |
| `LOCAL-06` | Claude Code CLI 版本 | 已通过：`2.1.268`；不代表可以接管 Desktop Code 会话。 |
| `LOCAL-07` | Swift 工具可调用 | 已通过：Swift 6.2 返回版本；没有编译应用或验证最低部署系统。 |
| `LOCAL-08` | 本机 Node 可调用 | 已通过：Node 22.22.3 返回版本；产品计划随包使用 Node 24.21.0。 |

下列检查没有通过为可用入口：

| 证据 ID | 实际观察 | 对方案的影响 |
| --- | --- | --- |
| `LOCAL-05` | `codex app-server daemon version` 退出码 1；默认 `~/.codex/app-server-control/app-server-control.sock` 不存在。 | 不能假定运行中的桌面应用提供该控制入口；同一 Session 接管须先完成 G0。没有为此启动新 daemon。 |

另有官方文档人工核对：Codex app-server 提供线程／轮次协议；Claude 深链只解决跳转与预填；Apple 提供防止空闲系统睡眠的断言和登录启动 API。这些引用在技术方案对应章节中，不计入脚本的 23 个通过项，也不标成真机功能通过。

### 2.1 菜单栏交互示意检查

1.1 版的浏览器示意检查，独立记录在 [menubar-preview-2026-09-18.json](research/menubar-preview-2026-09-18.json)。检查使用 Ego 浏览器实际点击、输入及辅助功能快照；记录含当时示意文件 SHA-256、执行时间和截图路径。没有连接真实 Agent，也没有创建原生 Session。

| 检查 | 结果 |
| --- | --- |
| Claude 示意会话发送后收起、重新展开 | 已通过：保留本轮消息。 |
| Codex／Claude 来回切换 | 已通过：恢复 Codex 原消息与未发送草稿。 |
| 点击 Cursor | 已通过：打开 Coming soon 展示配置。 |
| 关闭 Cursor 配置 | 已通过：仍为 Codex 原会话与草稿。 |
| 已固定三个图标时继续添加 | 已通过：提示先取消一个固定项。 |
| 更换为 Grok 固定图标 | 已通过：菜单栏展示 Coming soon，未启用会话能力。 |
| 736 px 与 320 px 浏览器视口 | 已目视检查：头像、消息和发送控件可见，未见横向溢出；消息间隙透出模拟桌面。 |

这六项交互检查与两种宽度的目视检查不计入前述 23 项研究结果，也不将 U01–U10 改为通过。跨窗口实时磨砂、原生焦点、真实会话复用和系统菜单栏命中区仍待真机验收。

### 2.2 默认／悬浮状态示意检查

1.2 版另存 [悬浮交互证据](research/menubar-hover-preview-2026-09-18.json)，保留源码 SHA-256 和默认／悬浮截图。浏览器实际操作通过以下五项：悬浮 Cursor 不改变当前会话、点击展开后的＋打开配置、取消全部固定项后仍保留＋、键盘 Enter 打开 Claude 后焦点保留、移出头像组后原会话仍打开。

默认／悬浮截图目视对比确认：只有＋向右展开，三个 Agent 头像位置不变。原生菜单栏命中区、系统“减少动态效果”集成及真实 Session 行为均未实测；U11、U12 仍为待执行。

上述是 1.2 版示意的历史记录。当前悬浮行为已改为三个 Agent 与＋整组展开，以现行交互规格和 U11 为准。

## 3. 验收准备与执行记录

准备独立 macOS 测试用户／测试设备，真实 iPhone（手机号）与 Mac（邮箱），可扫码的微信账号，已登录的 Codex 和 Claude。只用明确命名的测试会话和临时项目；不执行真实支付、外发邮件或不可逆业务操作。

项目夹具至少包含：两个同名不同目录的项目、一个非 Git 目录、两个重名 Session、无项目 Session、运行中和已归档 Session。消息夹具包含中文、英文、表情、换行、代码块、重复正文与不同原生消息 ID。

每条执行记录必须保存：用例 ID、应用 build、macOS／设备、Agent 版本／模式、通道、前置条件、实际结果、期望结果、日志或脱敏截图、执行时间。不能只保存一个绿色勾号。

以下各表状态全部为 **待执行**。表中的 D 组须分别在 Codex、Claude Code、Claude Chat、Claude Cowork 或对应合并体验运行，并记录哪些模式属于当前发布范围。未开放模式保留未执行状态，不改成通过。

## 4. 当前选择、命令和产品范围

| ID | 步骤 | 通过标准 | 优先级 |
| --- | --- | --- | --- |
| C01 | 清空桥接选择，从已绑定通道发送普通文本。 | 使用默认 Codex，仅建一个无项目 Session；回复含任务号和原生绑定信息。 | 阻断 |
| C02 | 无选择状态把默认 Agent 设为 Claude，再发送文本。 | 使用已验证的 Claude 默认模式，无项目创建；未就绪则明确等待，不退回 Codex。 | 阻断 |
| C03 | iMessage 发第一轮，微信发后续一轮，再回到 iMessage。 | 三轮使用同一原生 Session ID；各轮回复回原通道。 | 阻断 |
| C04 | 已绑定 Session 后重启桥接并闲置，再发送。 | 恢复同一 Agent／项目／Session；没有新建。 | 阻断 |
| C05 | 从 Codex 切到 Claude，分别覆盖 Claude 有／没有 active Session；再重复选择。 | 有则恢复其原生 Session 和项目；无则选择无项目，头像立即创建、远程命令下条普通消息创建；重复选择不重建。 | 阻断 |
| C06 | 切换已开放 Claude 模式；尝试尚不可用模式。 | 可用模式恢复自身 active Session；没有时选择无项目目标。不可用选择解释原因并保留原目标。 | 阻断 |
| C07 | 选择另一个项目，再选择无项目。 | 每次只改变选择；普通消息到来后才创建相应范围 Session。 | 阻断 |
| C08 | 选择 Agent，发送 `/project`，回复项目编号，再回复会话编号。 | 两级列表只显示编号与名称；选择项目后自动列出会话；Agent、模式、项目、Session 一次切换正确，下一条进入该原生 Session。 | 阻断 |
| C09 | 原生只有 Session 没有项目；执行项目／会话列表命令。 | 能列出并切换 Session；不会强制先创建项目。 | 阻断 |
| C10 | 在有项目及无项目状态分别发送 `/new`。 | 保留 Agent／模式／项目，延迟到下条消息再创建，旧任务仍可完成。 | 阻断 |
| C11 | 打开列表；分别测试有效／越界编号、普通句子、`//help` 和选择会话后的数字。 | 浏览不改选择；有效编号只选择；越界编号拒绝且不创建任务；普通句子退出选择并继续聊天；转义命令按原文交给 Agent；选择会话后的数字作为聊天内容。 | 阻断 |
| C12 | 存在当前 Session 时修改默认 Agent，再普通聊天。 | 当前 Session 不变；默认设置只作用于以后无选择的起点。 | 阻断 |
| C13 | 在 UI、键盘导航、IPC 和文本命令尝试选择所有 Coming soon 项，并配置其展示。 | 全部不可连接或设默认；Agent 展示配置可保存但不改变执行能力，Cursor 不能因此创建会话。 | 阻断 |
| C14 | 重名列表重新排序并翻页；另一通道切换目标；列表过期或解绑重绑后回复编号；删除／归档当前 Session。 | 编号保持快照映射，下一页连续编号；失效列表拒绝并提示刷新，不创建任务；不误选同名／新对象、不悄悄新建。 | 阻断 |

## 5. 消息路由、原子性与恢复

| ID | 步骤 | 通过标准 | 优先级 |
| --- | --- | --- | --- |
| R01 | 同一个原生事件重放 20 次，含进程重启。 | inbox／job 只有一个，Agent 不重复执行，最终回复分片不重复生成。 | 阻断 |
| R02 | 连续发送正文完全相同但原生 ID 不同的两条消息。 | 两条都处理，不能按正文哈希误删。 | 阻断 |
| R03 | 无 Session 时两通道同时注入 50 条有效消息。 | 只有一个创建预约和一个原生 Session，50 条按接收序号排队。 | 阻断 |
| R04 | 注入普通消息 A、切换命令、普通消息 B，同时让旧任务完成。 | A、B 分别使用接受时的目标；旧结果不跑到新 Session，回传通道保持原样。 | 阻断 |
| R05 | 在 inbox 提交后、job 创建前终止 helper。 | 重启补建一次 job；所有已发“已接收”的消息都有恢复记录。 | 阻断 |
| R06 | Agent 已接收但发送确认尚未入库时终止 helper。 | 重启核对原生状态；不能直接重发，无法核对时保持 uncertain。 | 阻断 |
| R07 | Session 实际创建成功但 ID 尚未入库时中断。 | 不再盲目创建第二个；能确认则重绑，否则显示待确认。 | 阻断 |
| R08 | 最终结果写入、outbox 创建、发送、发送标记落库的各边界断开。 | 结果不丢；明确未发送可重试，不确定送达不会重发整批。 | 阻断 |
| R09 | 让 Chat SDK handler 延后、去重或抛错，并使持久化写入失败。 | 原始 inbox 是事实来源；存储失败不推进通道 cursor；恢复后消息可处理。 | 阻断 |
| R10 | 队列达到产品上限／磁盘不足／排队超过 24 小时。 | 有明确背压或暂停；不回虚假的接受回执；过期执行等待继续，不静默丢弃。 | 阻断 |

## 6. iMessage 真机检查

| ID | 步骤 | 通过标准 | 优先级 |
| --- | --- | --- | --- |
| I01 | 在无 FDA／自动化权限的干净测试用户启动签名应用。 | 准确引导实际责任进程的授权；拒绝后不死循环、不假装已连接。 | 阻断 |
| I02 | 手机只用手机号、Mac 只用邮箱；点击开始配对，手机收到请求后核对发件邮箱并回复 OK，再对话 5 轮。 | 配对自动启用，无需配对码和本机二次确认；真实输入能被识别，结果发送到正确手机对话；记录原生字段脱敏样本。 | 阻断 |
| I03 | 同 Apple ID 启用消息同步，做 20 轮自发自收并观察同步副本。 | 每轮只执行一次；桥接回复不再进入 Agent；没有循环发消息。 | 阻断 |
| I04 | 对相同文本同时制造真实新输入和出站同步副本。 | 新输入保留、副本排除；仅靠 isFromMe／正文哈希的实现不能通过。 | 阻断 |
| I05 | 停止桥接期间发 3 条消息，重启，同时再发 1 条。 | 补扫与实时衔接四条都处理一次；首次绑定仍不执行旧历史。 | 阻断 |
| I06 | 测试 tapback、已读变化、编辑、撤回、附件延迟下载。 | 非新用户请求不触发任务；不完整附件等待就绪或明确报错。 | 高 |
| I07 | iMessage 不可用或原生会话变成 SMS。 | 不意外回退短信发送，显示通道错误。 | 阻断 |
| I08 | 连发两个附件，使第二个失败。 | 记录部分成功，修复后不重发第一个，也不把失败标成全部成功。 | 高 |
| I09 | osascript 超时但消息可能已出现于 Messages。 | 内部／外部重试不会叠加成重复发送；状态保持待确认。 | 阻断 |
| I10 | 来自其他联系人、群聊或另一个邮箱的消息到达。 | 不创建任务、不泄露当前项目／Session；不发送用户任务结果给陌生人。 | 阻断 |

## 7. 微信真机与协议检查

| ID | 步骤 | 通过标准 | 优先级 |
| --- | --- | --- | --- |
| W01 | 首次扫码、二维码过期刷新、扫码取消后重新扫码。 | 原生 UI 能完成；凭据只入 Keychain，只有绑定成功才显示可用。 | 阻断 |
| W02 | 使登录过期／token 被撤销，随后重新授权。 | 账户进入待登录，停止无效重试；恢复后保留 Agent Session 和未完成任务。 | 阻断 |
| W03 | 协议夹具中混入其他 peer／account 的 context token。 | token 不串用，陌生身份不成为 owner；回复只使用对应原通道上下文。 | 阻断 |
| W04 | 接收 batch 中途崩溃，分别在 persistInbound 和 cursor 更新前后注入故障。 | 已收到消息可恢复、不漏、不重复执行；写盘失败不能前移 cursor。 | 阻断 |
| W05 | 长轮询正常超时、断网、网络切换、服务器限流。 | 超时不误判注销；退避重连无忙循环，其他通道可继续工作。 | 高 |
| W06 | HTTP 200 携带非零 ret／errcode、缺少 context token、发送未知结果。 | 区分网络和业务错误；不把 HTTP 成功当发送成功，不假定 client_id 严格幂等。 | 阻断 |
| W07 | 回复已缓存消息和仅含未知服务端 ID 的引用。 | 已知引用正确关联；未知引用明确不可读，不编造内容。 | 高 |
| W08 | 退出微信绑定，并在任务完成时尝试回传。 | 立即停止该通道发送；结果留待本地查看，不自动发到另一个通道。 | 阻断 |

## 8. 桌面 Agent 连接与兼容性

| ID | 步骤 | 通过标准 | 优先级 |
| --- | --- | --- | --- |
| D01 | 由桌面手动创建命名测试 Session，桥接枚举它；双向交替发消息。 | 原生 ID 一致、双方均可见同一对话；单独 CLI 新建／复制历史不算通过。 | 阻断 |
| D02 | 从无选择的桥接创建无项目 Session，再到桌面查看并继续。 | 真正是同一无项目 Session；不进入最近项目、不隐式使用 Home。 | 阻断 |
| D03 | 枚举大量项目／Session，含重名、归档、无项目和分页。 | 有唯一定位依据；可获取全量或明确 partial，不把首屏当全集。 | 阻断 |
| D04 | 发送产生长输出与多次工具调用的请求。 | 只转发用户可见内容；最终结果完整，以完成事件／可靠 UI 状态判定。 | 阻断 |
| D04a | 同一任务产生公开过程说明、正文增量、重复完成事件、思考及工具事件；执行中切换目标／停止／断线。 | 正文实时更新同一气泡；通道按完整消息发送一次；隐藏思考、工具和子任务；原目标不变；停止或异常保留部分正文并显示状态。[179 项服务／49 项原生及真实运行时检查通过](verification/streaming-2026-09-19.md)，手机端实际送达另验。 | 阻断 |
| D05 | Agent 忙碌时继续发送，再按任务 ID 取消。 | 不把后续消息送到别的 Session；取消命中指定 turn，结果状态一致。 | 阻断 |
| D06 | 移除原生接口／关闭 Agent／退出登录。 | 明确不可用或等待；不偷偷新启 CLI、不改用另一个 Agent。 | 阻断 |
| D07 | 自动化定位后用户手动切窗口、切 Session、改输入框。 | 发送前重新核验，冲突则暂停；不覆盖用户输入或误发。 | 阻断 |
| D08 | 更新 Agent 版本／修改语言／改变窗口大小；模拟控件定位不唯一。 | 可验证版本正常；不兼容时停用发送并保留队列，不能按坐标猜测。 | 阻断 |

## 9. 审批与安全边界

| ID | 步骤 | 通过标准 | 优先级 |
| --- | --- | --- | --- |
| A01 | Agent 发出一次工具审批，分别测试同意和拒绝。 | 展示真实操作摘要；只回复对应原生 request，原有权限策略仍生效。 | 阻断 |
| A02 | 两项审批同时待答，在另一通道回复其中一个短码。 | owner、任务和 request 绑定正确；不会批准最近显示但不同的审批。 | 阻断 |
| A03 | 重放已批准短码、使用过期短码和错误 sender。 | 全部无效，不产生新授权。 | 阻断 |
| A04 | 出现需 Mac 本地确认且不能远程代理的授权。 | 提示本地确认，任务保持等待，不自动点击“始终允许”。 | 阻断 |
| A05 | 审批期间取消任务、关闭会话、断线并重启。 | 原生请求已失效则撤销本地审批；不恢复为新的可批准操作。 | 阻断 |
| A06 | 在 Agent 输出、附件和引用中嵌入 `/approve`、切换命令。 | 只有已授权用户的新入站命令能控制桥接，Agent 输出不被再次解析执行。 | 阻断 |
| S01 | 测试配对过期、错误回复、重复点击、取消、撤销绑定和昵称伪装；iMessage 混入旧 OK、其他单聊及来自本机的 OK。 | 没有身份提升；只接受本机发起的当前流程及指定原生身份；不会因重复开始自动多次发信。 | 阻断 |
| S02 | 导出诊断并检查日志、数据库、崩溃记录和进程参数。 | token 不明文泄露；默认日志无消息正文及个人标识，诊断已脱敏。 | 阻断 |
| S03 | 附件使用路径穿越、符号链接、伪扩展名、超大数据。 | 不能读写或回传授权范围外文件；大小按实际字节检查。 | 阻断 |
| S04 | 查看监听端口、从其他用户进程调用 helper、重复启动两个实例。 | 不暴露公网控制接口；不接受未建立的 IPC 控制；只一个通道消费者。 | 阻断 |

## 10. 锁屏、休眠与生命周期

| ID | 步骤 | 通过标准 | 优先级 |
| --- | --- | --- | --- |
| L01 | 开关保活并查看 `pmset -g assertions`。 | 开启持有正确空闲系统睡眠断言；关闭／暂停全部通道后释放，不阻止显示器休眠。 | 阻断 |
| L02 | 开启保活后真实锁屏 20 分钟，两通道各发消息。 | 能接收的消息及时持久化；需要 GUI 的任务显示等待解锁，不能声称已执行。 | 阻断 |
| L03 | 在等待解锁时切目标，再解锁。 | 每项任务按接受时的冻结目标继续，绝不全部送进最后选中的 Session。 | 阻断 |
| L04 | 合盖／手动睡眠后唤醒。 | 不宣称睡眠期间在线；唤醒后恢复 cursor、目标和任务，无重复执行。 | 高 |
| L05 | 注销／重启／快速用户切换／Keychain 暂不可用。 | 登录前不承诺服务；凭据不可用则等待，不将 token 写明文绕过。 | 高 |
| L06 | 关闭窗口、退出主应用、强制结束 helper。 | 关窗口仍运行；退出停止消费并释放断言；helper 崩溃按上限恢复。 | 阻断 |
| L07 | 服务接口可收消息，但任务随后要求 GUI 或本地确认。 | 分别展示传递成功与执行阻塞，不把接口可用当任务完成。 | 阻断 |
| L08 | 关闭保活、切到电池模式并恢复供电。 | 尊重系统电源行为和用户开关，状态文案准确，不自动开启或更改全局睡眠设置。 | 高 |

## 11. 附件与消息格式

| ID | 步骤 | 通过标准 | 优先级 |
| --- | --- | --- | --- |
| F01 | 发送中文、英文、表情组合、长代码块及超长答复。 | 字素不截断，代码块闭合，分片顺序正确，完整结果可取得。 | 高 |
| F02 | 测试单文件限制的少 1 字节／等于／多 1 字节；5／6 个附件和合计限制。 | 边界结果明确；拒绝的附件不会触发空请求或半个任务。 | 高 |
| F03 | 接收图片与通用文件，转给支持附件的目标，再回传生成文件。 | 内容哈希／大小一致，文件名与类型正确；目标不支持时明确拒绝。 | 高 |
| F04 | 发送语音／视频等尚未开放类型或空消息。 | 明确说明支持范围，不把文件名当正文、不凭空转录，不创建空 Session。 | 高 |
| F05 | 让临时文件清理运行，分别包含已完成与未完成任务。 | 到期完成附件清理，未完成输入不删除；无长期滞留秘密文件。 | 高 |
| F06 | 微信／iMessage 显示同一选择和审批模板。 | 两者均有可实际输入的操作命令，卡片降级不丢 Actions。 | 阻断 |

## 12. 安装、升级与非功能检查

| ID | 步骤 | 通过标准 | 优先级 |
| --- | --- | --- | --- |
| P01 | 在没有 Node／npm／开发工具的干净 Mac 安装。 | 内置 Node 与 better-sqlite3 可加载，应用正常启动；不要求用户修 PATH。 | 阻断 |
| P02 | 检查签名、公证票据、Gatekeeper，启动嵌入式 helper。 | 分发包可通过系统检查，所有需要签名的嵌入对象完整；开发机测试不替代此项。 | 阻断 |
| P03 | 开关登录启动并重登；关闭设置窗口和退出应用。 | 登录启动遵循用户选择，菜单栏生命周期与文案一致，无重复实例。 | 阻断 |
| P04 | 升级签名应用、撤销权限、重新授权。 | 权限状态可靠检测和恢复；不会靠开发终端权限勉强运行。 | 阻断 |
| P05 | 数据库迁移中止、损坏备份、旧版应用回退。 | 迁移原子、可恢复；不兼容降级拒绝启动写入，不丢绑定和未完成任务。 | 阻断 |
| P06 | 运行发布所列 macOS／架构／Agent 版本兼容矩阵。 | 仅对已执行组合宣称支持；至少覆盖当前支持版本与前一受支持版本。 | 阻断 |
| N01 | 连接两通道空闲 15 分钟采样资源。 | 目标为两进程合计 RSS ≤200 MiB、平均 CPU <1%；超标有分析及明确发布决定。 | 高 |
| N02 | 在协议夹具注入 1,000 条消息，记录接受耗时。 | 本地持久化接受 p95 <500 ms，无静默丢弃；不把模型执行时间纳入。 | 高 |
| N03 | 真机测已连接通道的接收回执与正常回复。 | 记录平台网络与本地耗时；正常网络下接收回执目标 p95 <3 秒，异常状态可解释。 | 高 |
| N04 | 连续运行 24 小时，穿插断网、Agent 重启、helper 崩溃和多次锁屏。 | 无循环消息、无人知晓的任务丢失或持续资源增长；未决任务可恢复／解释。 | 阻断 |
| N05 | 中文／英文界面、长名称、辅助功能阅读、键盘导航。 | 当前目标与连接状态可辨识，Coming soon 不可误选，长 ID／文本不遮挡关键操作。 | 高 |

## 13. 菜单栏交互与 G0 实测证据

2026-09-18 动态宽度修复：回归测试先复现默认及收拢后仍占 102 pt 的问题。图标组现在逐帧同步系统 `NSStatusItem.length`，三个 Agent 加＋的实际按钮宽度为 72 → 102 → 72 pt，不再保留右侧展开空白。37 项 Swift 测试通过，覆盖原生状态按钮宽度、展开后的横向范围、状态刷新、固定项数量变化、零固定项、收拢途中重新移入及动画中的点击位置。跨屏、菜单栏自动隐藏与减少动态效果仍按 U09、U12 真机验收。

2026-09-18 菜单栏点击修复：在 macOS 27.0（26A428）捕获到连续真实点击，事件位置始终为状态窗口坐标 `(59, 16.5)`，转换后为头像组坐标 `(51, 11)`，落在展开后的两个图标之间，因而全部命中空白。同期窗口实时鼠标位置随实际点击变化。已改为从状态窗口读取实时位置；回归用例固定上述错误事件坐标，移动测试窗口的鼠标位置，验证 Codex、Claude、Cursor、＋分别触发。该用例修复前失败、修复后通过；原生测试共 30 项通过，并覆盖同一浮层连续五轮收起与重开。签名应用更新后，用户报告“好了”，确认点击已恢复；该反馈不等同于下方所有菜单栏组合验收。

同日会话浮层修复：原生 WindowServer 命中检查复现了全透明消息空白不接收鼠标事件；滚动区域增加 0.004 alpha 后，空白点命中会话窗口，像素透明度保持不超过 0.01，根视图外缘仍完全透明。短消息布局回归也先失败后通过，验证用户气泡贴右、AI 气泡贴左且不强撑宽度。消息卡和输入框统一使用更明亮的局部磨砂与细白边，输入框增加留白。全部 32 项 Swift 测试通过，release 构建与签名校验通过；已更新并重启应用（UUID `CC7AC5E5-7EB4-3CD9-A572-3B58B8A46BC4`）。通过原生 UI 操作确认透明空白处双向滚动、滚动条不显示，以及短消息左右位置和输入框外观。暗色／高对比壁纸、减少透明度、跨屏等组合仍按 U08、U09 完整验收。

后续视觉调整：浮层采用淡磨砂背景，移除标题行，气泡与输入框使用乳白表面和柔和阴影。为消除左右和下方的矩形边界，背景改用 `NSVisualEffectView.maskImage` 直接羽化原生材质，外缘完全透明；新增回归用例修复前因原生遮罩缺失失败，修复后四边透明度和中间过渡均通过。消息区顶部 48 pt 模糊淡出，底部 32 pt、两侧 12 pt 渐隐；列表首尾和窗口四周留出空间，避免消息及阴影硬切。原滚动命中与左右布局用例继续通过，当前全部 37 项 Swift 测试通过。release 构建与签名校验通过，已更新并启动应用（UUID `A2917942-5E6D-3DCE-A0B9-B0F67BB924BA`）；原生窗口截图检查了左右与底部渐隐、卡片阴影和无滚动条，并实际滚动至列表底部，确认最新消息能完整显示。

新增菜单栏与会话交互用例（均待执行）：

| ID | 步骤 | 通过标准 | 优先级 |
| --- | --- | --- | --- |
| U01 | 安装后观察菜单栏，分别使用鼠标与辅助功能访问各入口。 | Codex、Claude、Cursor、＋为紧凑整体，各自有独立命中区及名称；Cursor 明确 Coming soon。 | 阻断 |
| U02 | 点＋，修改固定项、名称与排序并重启。 | 配置持久化，＋始终可访问；不创建会话、不改变全局目标。 | 阻断 |
| U03 | 点击 Cursor／其他 Coming soon 头像，再通过＋修改展示配置。 | 头像显示透明浮层与磨砂状态卡，不打开主窗口；不能连接、切换当前 Agent、设默认或创建 Session。 | 阻断 |
| U04 | 点击已有 active Session 的 Agent，收起后重复展开。 | 同一原生 ID、同一历史与草稿；无重复 Session。 | 阻断 |
| U05 | 没有 active Session 时快速连续点头像 20 次，创建过程中改点另一个 Agent。 | 每个预约只创建一次无项目 Session；迟到结果不抢回当前界面和选择。 | 阻断 |
| U06 | 头像打开 A，再从微信输入；改点 B，再从 iMessage 输入。 | 新消息进入成功打开的当前目标；之前的任务与回复路由保持不变。 | 阻断 |
| U07 | 从本机面板发送消息，收起面板再恢复，期间原生 Agent 继续运行。 | 同一原生会话；本机结果不广播社交通道，收起不取消任务。 | 阻断 |
| U08 | 在明／暗／高对比壁纸上检查消息空白和输入区，并在空白处上下滚动。 | 无标题行；背景轻磨砂且四周羽化，消息区左右和底部无矩形硬切线；顶部消息逐渐模糊淡出；用户气泡贴右、AI 气泡贴左，短消息保持自然宽度；消息卡与输入框有明显的乳白磨砂及柔和阴影；空白区可滚动且无滚动条；减少透明度关闭背景模糊、局部卡片保持可读。 | 阻断 |
| U09 | 中文输入、切换显示器、菜单栏自动隐藏、屏幕边缘和刘海区域。 | 焦点稳定、输入法可用、面板不越界；后台 AX 操作不持续抢走输入焦点。 | 阻断 |
| U10 | 加载分页历史，原生应用与远程通道同时产生新消息。 | 面板与原生 Session 一致，消息按原生 ID 去重，partial 状态真实，不用本地假历史替代。 | 阻断 |
| U11 | 从组外移入头像和间隙，再移到＋并移出，重复 20 次；观察当前目标。 | 默认四个圆形按钮依次重叠，每处均有透明圆弧隔离；悬浮后整组展开为完整圆形，移开后整组恢复重叠；系统实际占位为 72 → 102 → 72 pt，允许相邻项随系统布局移动，不反复抖动；不创建／切换会话，不因离开而关闭面板。 | 阻断 |
| U12 | 键盘访问、减少动态效果、取消全部固定项，以及动画途中点击头像／＋。 | 焦点清晰且不丢失；减弱动效设置生效；零固定项仍有完整＋；点击始终进入对应入口，不误命中其他 Agent。 | 阻断 |

| 验证目标 | 最小证据 | 失败后的处置 |
| --- | --- | --- |
| 桌面同一 Session | 原生 ID、桌面前后截图／记录、桥接事件，明确没有另起替代会话。 | 评估另一条已授权桌面适配路径；不能用 CLI 成功顶替。 |
| iMessage 无回声 | 20 轮真实输入与输出计数、脱敏 sender／recipient／GUID 对照。 | 保留不支持状态，定位实际同步语义。 |
| 微信可靠入站 | 扫码结果、cursor／inbox 提交点与断点恢复记录。 | 修复适配器边界后重测。 |
| 锁屏行为 | 真正锁屏时间、断言状态、消息到达与 waiting_unlock 记录。 | 只宣称已实测的后台能力。 |
| 签名包权限 | 干净用户上的授权记录及嵌入式运行时启动结果。 | 修正 TCC 归因／打包方式，再验收。 |

## 14. 发布判定

需求追踪：

| 方案需求 | 主要验收项 |
| --- | --- |
| R01 macOS 本地应用 | P01、P02、P06、N01。 |
| R02 通道范围 | C13、I01–I10、W01–W08。 |
| R03 Agent 范围和默认值 | C01、C02、C05、C13。 |
| R04 Claude 模式分阶段 | C06、D01–D08 分模式执行。 |
| R05 默认无项目新 Session | C01、C02、D02、R03、R07。 |
| R06 保持同一 Session | C04、C10、C12、C14、R05–R07。 |
| R07 跨通道共享目标和原路回复 | C03、R04、W08。 |
| R08 项目与 Session 列表／切换 | C07–C09、C14、D03。 |
| R09 iMessage 身份和回声 | I02–I04、I10。 |
| R10 锁屏后台保活 | L01–L08。 |
| R11 指定接入方式 | 依赖／源码证据、I 组、W 组。 |
| R12 统一格式与操作 | F01、F03、F06。 |
| R13 菜单栏头像组与配置 | U01–U03、C13。 |
| R14 恢复或新建 active Session | U04–U07、U10、C05。 |
| R15 轻磨砂背景、顶部淡出与局部磨砂 | U08、U09。 |
| R16 默认与悬浮状态 | U11、U12。 |

研究检查全部通过不是发布条件。当前范围内所有“阻断”用例通过、所有高优先级缺陷已有结论、真机证据可复核后，才允许发布对应通道／模式。

Codex + 两通道闭环可作为内部阶段版本；对外声明支持 Claude Code、Chat、Cowork 时必须分别补齐对应 D 组和相关回归。最终技术验收中仍不可用的能力不能通过修改文案或复制 Session 内容来“通过”。

本轮未进行以上真机发送、登录或桌面操作，保持用户现有会话、系统权限和电源设置不变。
